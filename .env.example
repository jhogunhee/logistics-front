# 실제 값은 .env.local 에 넣는다 (이 파일은 형식만 보여주는 견본이라 커밋된다).
# .env / .env.* 는 .gitignore가 막으므로 .env.local 에 넣으면 커밋될 일이 없다.

# wms-backend 주소. 비워두면 http://localhost:8080 (src/utils/axios.js 기본값)
VITE_API_URL=http://localhost:8080
